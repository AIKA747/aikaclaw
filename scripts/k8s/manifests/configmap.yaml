apiVersion: v1
kind: ConfigMap
metadata:
  name: aikaclaw-config
  labels:
    app: aikaclaw
data:
  aikaclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.aikaclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "AikaClaw Assistant",
            "workspace": "~/.aikaclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # AikaClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
