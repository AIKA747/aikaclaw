apiVersion: v1
kind: Service
metadata:
  name: aikaclaw
  labels:
    app: aikaclaw
spec:
  type: ClusterIP
  selector:
    app: aikaclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
