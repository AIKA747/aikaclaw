---
summary: "CLI reference for `aikaclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `aikaclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `aikaclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `aikaclaw clawbot qr` (same behavior as [`aikaclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `aikaclaw clawbot qr` -> `aikaclaw qr`
